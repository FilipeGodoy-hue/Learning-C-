#include <stdio.h>

int main(){
    char name[]= "Filipe";
    char place[] = "Recife";
    char university[] = "UPE";
    printf("%s is living in %s and studying\t\t\tcomputer engineering at %s\n", name, place, university);
    
//    printf("How old \t \t \t are you? ");
//    we are studying the \t \t \t escape sequence, so we can use it to create a tab space in the output, as shown in the line above. We can also use it to create multiple tab spaces, as shown in the line above. The \t escape sequence is used to create a horizontal tab space in the output. It is equivalent to pressing the Tab key on the keyboard. We can use it to align text in the output, as shown in the line above. We can also use it to create multiple tab spaces, as shown in the line above.
     
     return 0;

}